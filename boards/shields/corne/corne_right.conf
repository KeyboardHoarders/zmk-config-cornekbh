# Right-half only: use ZMK built-in widgets instead of the nice!view
# custom vertical widget. This makes the right display use the
# `status_screen` defined in `corne_right.overlay`.

CONFIG_ZMK_DISPLAY_STATUS_SCREEN_BUILT_IN=y
